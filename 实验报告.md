# 猫狗图像分类实验报告

> 对应《猫狗分类实验指导书》六个步骤。全部代码在本仓库中可直接运行，
> 每个数字都能在 `results/*.json` 与 `results/logs/*.log` 里查到原始出处。

---

## 一、实验环境与数据集

### 1.1 环境

| 项目 | 配置 |
|---|---|
| 操作系统 | Windows 10（10.0.26100） |
| CPU / GPU | 本机有 NVIDIA RTX 4060 Laptop（8GB），**本实验未启用** |
| Python | 3.8.6（`C:\Program Files\python`） |
| PyTorch | 2.4.1+cpu（torchvision 0.19.1+cpu） |
| 其他 | numpy 1.24.4 / scikit-learn 1.3.2 / Pillow 10.4.0 / gradio 4.44.1 |
| 设备 | `device='cpu'`，与指导书一致 |

关于 GPU：本机确实有独立显卡，但指导书全部代码与超参（包括每轮用时的预期）都建立在
CPU 上，且**控制变量**要求各步骤可比。因此这里刻意保持 `device='cpu'`，
用 GPU 只会在几十秒的实验上再省几十秒，却会让"每轮用时"这一列与指导书失去可比性。
（如果想切 GPU，`common.get_device(prefer_gpu=True)` 已经留好了开关。）

依赖没有装进系统 Python，而是装在仓库内的 `.venv2`，预训练权重缓存也固定在
仓库内 `.torch_cache`（由 `common.py` 设置 `TORCH_HOME`），做到"实验结束删目录即还原"。
安装步骤见 `README.md`。

### 1.2 数据集

100 张 JPEG，`cat.1~50.jpg` + `dog.51~100.jpg`，标签藏在文件名里，
与指导书 0.2 节 `FileNameDataset` 的约定一致。

用 `tools/check_images.py` 做过一次完整体检：

| 项目 | 结果 |
|---|---|
| 可解码 | 100/100（无损坏、无截断） |
| 色彩模式 | 全部 RGB（无需额外通道处理） |
| 分辨率范围 | 宽 105~500 px，高 93~500 px，中位数 431×374 |
| 短边 < 100 px | 2 张（`cat.48.jpg` 139×93、`dog.69.jpg` 128×95） |

**这只说明统一 `Resize((224,224))` 是必需的**：原图长宽比、分辨率差异极大，
不统一尺寸根本没法组 batch；而小图被放大、大图被压缩，本身也会损失信息
（后面第五步的错分样本里有相当一部分正是这类图）。

数据划分有两套（都固定 `seed=42`，都是 80 训练 / 20 验证）：

* **txt 索引版**（第一、二步用，指导书 1.2 节 `dataloader()` 的写法）：
  生成 `results/trainDatasets.txt`（80 行，cat 39 / dog 41）与
  `results/testDatasets.txt`（20 行，cat 11 / dog 9）。
* **`common.get_loaders` 版**（第三、四、五步用，指导书 0.2 节）：
  用 `random_split` + `torch.Generator().manual_seed(42)`。

> ⚠️ 两套划分的**具体图片不完全相同**，这是指导书本身的安排（1.2 节和 0.2 节
> 各给了一套数据管线）。所以"第一步 vs 第二步"内部可比、
> "第三/四/五步"内部可比，跨大步比较时要记住这一点。报告里所有结论都只在同一步内部下判断。

---

## 二、第一步：手动搭建 AlexNet，从零训练

**代码**：`step1_scratch.py`　**日志**：`results/logs/step1_scratch.log`

### 2.1 模型

严格照抄指导书 1.2 节的精简版 AlexNet（去掉双 GPU 设计）：

```
输入 3×224×224
Conv 11×11/4, pad 1, 96 → ReLU → MaxPool 3×3/2      → 96×54×54 → 96×26×26
Conv 5×5, pad 2, 256   → ReLU → MaxPool 3×3/2       → 256×26×26 → 256×12×12
Conv 3×3, pad 1, 384   → ReLU                        → 384×12×12
Conv 3×3, pad 1, 384   → ReLU                        → 384×12×12
Conv 3×3, pad 1, 256   → ReLU → MaxPool 3×3/2        → 256×5×5
Flatten → 6400
Linear 6400→4096 → ReLU → Dropout 0.5
Linear 4096→4096 → ReLU → Dropout 0.5
Linear 4096→2
```

参数量 **46,755,202**。权重初始化用指导书的 `xavier_uniform_`。
超参：`batch_size=16, lr=1e-4, epochs=30, device='cpu'`，Adam + CrossEntropyLoss。

### 2.2 结果

| 指标 | 值 |
|---|---|
| 第 1 轮准确率 | 0.5500 |
| 第 10 轮准确率 | 0.5500 |
| 第 20 轮准确率 | 0.8500 |
| 第 30 轮（最终）准确率 | **0.8500** |
| 30 轮最高准确率 | 0.9000（第 27 轮） |
| 训练损失 | 0.7584 → **0.0012** |
| 测试损失 | 0.6853 → **0.6320**（第 19 轮一度冲到 1.04） |
| 总用时 | 92.4 s（平均 3.1 s/轮） |

验证集混淆矩阵（正类 = 狗，TN=10 FP=1 FN=2 TP=7）：

|  | 预测猫 | 预测狗 |
|---|---|---|
| **真猫** | 10 | 1 |
| **真狗** | 2 | 7 |

### 2.3 结论（"请验证"）

指导书预期"小数据集从零训练学不出通用视觉特征，准确率 0.65~0.80"。
本次实测 **0.85（最好一轮 0.90）**，略高于预期区间，但**结论方向完全一致**，
而且证据比准确率本身更有说服力：

1. **训练损失塌到 0.0012，测试损失却卡在 0.63 不降**——这是典型的过拟合：
   46.7M 参数的模型对 80 张图来说容量过剩，它已经能把训练集"背下来"（几乎零误差），
   却没有学到能迁移到新图的特征。测试损失在第 19 轮甚至反弹到 1.04，
   说明从零训练的过程本身也不稳定。
2. **准确率在 0.55↔0.90 之间来回跳**，最后 10 轮在 0.75~0.90 震荡，
   没有收敛到一个稳定水平——20 张验证集上 1 张就是 5%，抖动幅度就是这个量级。
3. 为什么比预期高：本数据集不是 Fashion-MNIST 那种低分辨率灰度图，而是
   224×224 的彩色真实照片，猫狗在纹理、颜色、轮廓上的差异比"衣服类别"更显著，
   所以随机初始化 + XGB 级别的数据量也能蒙到 0.85。这恰恰说明**光看准确率会误判**，
   必须结合训练/测试损失的背离一起看。

---

## 三、第二步：加载 ImageNet 预训练参数（迁移学习）

**代码**：`step2_pretrained.py`　**日志**：`results/logs/step2_pretrained.log`

### 3.1 做法

新建 `AlexNetManual`，层名与 `torchvision.models.alexnet` 完全一致
（`features` / `avgpool` / `classifier`，`classifier[6]` 是最后一层）。
按指导书三步走：先建 1000 类结构 → `load_state_dict` 灌官方权重 → 把
`classifier[6]` 换成 `nn.Linear(4096, 2)`。

* **做法一（默认）**：`param.requires_grad = name.startswith("classifier")` 冻结卷积。
* **做法二（进阶）**：删掉冻结，`lr=1e-5, epochs=5` 全量微调。

### 3.2 结果

| 方案 | 可训练参数 | 准确率 | 精确率 | 召回率 | F1 | 总用时 | 每轮用时 |
|---|---|---|---|---|---|---|---|
| 冻结卷积（30 轮, lr=1e-4） | 54,542,338 | **1.0000** | 1.0000 | 1.0000 | 1.0000 | 72.8 s | 2.4 s |
| 全部微调（5 轮, lr=1e-5） | 57,012,034 | 0.9000 | 0.8990 | 0.8990 | 0.8990 | 16.0 s | 3.2 s |

冻结方案逐轮：**0.90 → 0.95 → 1.00**，第 3 轮起就稳定在满分，之后 27 轮训练损失
从 0.024 一路降到 0.0000，验证准确率再没动过——**3 轮就收敛了，剩下 27 轮纯属浪费**。
微调方案逐轮：0.65 → 0.75 → 0.85 → 0.90 → 0.90，**还在稳定上升，明显没训够**。

### 3.3 结论

- **"知识从哪来"比"训练多久"重要**：第一步从零练 30 轮 = 0.85；
  第二步加载预训练权重后 **3 轮就到 1.00**。同一个网络结构、同一份数据、
  同样的学习率，唯一的差别就是"初始权重是不是 ImageNet 学出来的"。
  预训练的卷积核已经会提取边缘→纹理→部件这类通用视觉特征，我们要教的只剩
  "这些特征怎么投票成猫或狗"。
- **冻结 vs 微调的对比要小心解读**：表面上"冻结 1.00 > 微调 0.90"，
  但公平的结论是 **不是微调更差，而是 5 轮 + lr=1e-5 的训练量远远不够**——
  微调把 57M 个参数全部打散重学，前 2 轮准确率一度掉到 0.65，
  这正说明它从预训练解空间里"跑出去了"；曲线还在上升，再训 20~30 轮大概率反超。
  这正是指导书说的"解冻全部层…但数据少时容易过拟合，CPU 上也慢"。
- **一个容易被忽略的细节**：AlexNet 的参数量几乎全在全连接层。
  `classifier` 占 54.5M / 57.0M ≈ **95.6%**，卷积层只有 2.5M。
  所以"冻结卷积层"实际冻结的参数量不到 5%，训练开销几乎没省下来
  （2.4s/轮 vs 微调 3.2s/轮，差的是反向传播的次数而不是参数量）。
  这也解释了为什么冻结方案每轮只比全量微调快一点点。
- 还有个不可忽略的差异：第一步的 AlexNet 是指导书 1.2 的 96/256/384/384/256 版本，
  第二步为了对齐官方权重换成了 64/192/384/256/256 版本（第一层 padding 也从 1 变成 2）。
  **两步的网络结构并不完全相同**，所以 0.85 vs 1.00 的差距里混了结构差异；
  但"从零训练过拟合、预训练 3 轮收敛"这个定性结论不受影响。

---

## 四、第三步：数据增强（四组横向对比 + 多种子重复实验）

**代码**：`step3_augment.py`（单次划分 A/B/C/D）、`step3_erasing_cv.py`（5 种子重复）
**日志**：`results/logs/step3_augment.log`、`results/logs/step3_erasing_cv.log`

### 4.1 实验设计

指导书第三步的标题是"三个实验横向对比"，但参考代码只给了一种增强配置。
为了让"对比"名副其实，这里**固定其他所有变量**——同一份 80/20 划分（seed=42）、
同一个手写 ResNet18（层名与 torchvision 完全一致，见 `step1_scratch.ResNet18Manual`）、
同一份 ImageNet 预训练权重、同样 5 epoch / lr=1e-3 / 冻结卷积只训 `fc`——
只改**训练集增强强度**这一个变量：

| 组 | 训练集 transform |
|---|---|
| A 无增强 | `Resize(224,224)` |
| B 基础增强 | `Resize(256,256)` → `RandomResizedCrop(224, scale=(0.7,1.0))` → `RandomHorizontalFlip()` |
| C 强增强 | B + `ColorJitter(0.2,0.2,0.2)` + `RandomRotation(10)` |
| D 强增强+擦除 | C + `RandomErasing(p=0.5, scale=(0.02,0.2))`（思考题 2 追加，见第九节） |

> `RandomErasing` 必须排在 `ToTensor()` 之后（它只接受 Tensor）；这里进一步排在
> `Normalize` 之后，`value=0` 填充的就是"ImageNet 均值色"，相当于挖掉一块中性灰斑，
> 而不会引入"黑色矩形"这种训练集里本不存在的新伪影。

验证集**始终只用干净的 `Resize(224,224) + Normalize`**，这一点四组完全一致，
否则评估指标不可信。

### 4.2 结果

| 配置 | 准确率 | 精确率 | 召回率 | F1 | 每轮用时 | 平均置信度 | 置信度>0.9 |
|---|---|---|---|---|---|---|---|
| A 无增强 | 0.9000 | 0.9231 | 0.8889 | 0.8958 | 1.9 s | 0.8134 | 7/20 |
| B 基础增强 | 0.8000 | 0.8667 | 0.7778 | 0.7802 | 2.9 s | 0.8402 | 12/20 |
| C 强增强 | 0.9000 | 0.9231 | 0.8889 | 0.8958 | 3.3 s | 0.8270 | 9/20 |
| D 强增强+RandomErasing | **0.9500** | 0.9583 | 0.9444 | 0.9488 | 3.4 s | 0.7983 | 7/20 |

（可训练参数都是 1,026，即 `fc` 的 512×2 + 2；每轮用时增加纯粹来自增强的
CPU 图像变换开销：1.9s → 2.9s → 3.3s → 3.4s。A/B/C 三组的数字与首次运行
**逐位一致**，说明固定种子后整套流程是可复现的。）

**但这张表只有 20 张验证集，不能用来下结论。** 于是又做了 5 个数据划分种子的
重复实验（`step3_erasing_cv.py`，4 配置 × 5 种子 × 5 epoch），把"配置之间的差异"
和"划分之间的波动"放在一起比：

| 配置 | 准确率（5 折均值 ± 标准差） | 最低 | 最高 | 逐折准确率 |
|---|---|---|---|---|
| A 无增强 | 0.9000 ± 0.0612 | 0.85 | 1.00 | 0.90 / 0.90 / 1.00 / 0.85 / 0.85 |
| B 基础增强 | 0.8800 ± 0.0570 | 0.80 | 0.95 | 0.80 / 0.90 / 0.95 / 0.90 / 0.85 |
| C 强增强 | **0.9100 ± 0.0652** | 0.80 | 0.95 | 0.90 / 0.80 / 0.95 / 0.95 / 0.95 |
| D 强增强+RandomErasing | 0.8900 ± **0.0418** | 0.85 | 0.95 | 0.95 / 0.90 / 0.85 / 0.90 / 0.85 |

**四组的均值全部落在 0.88~0.91 这 3 个百分点的区间里，而每一组自己的折间标准差
就有 4~7 个百分点**——配置之间的差异**小于**同一配置换一份划分的波动。
换句话说：在 100 张图的数据集上，"增强强度"对准确率的影响**测不出来**，
单次划分看到的 0.95 vs 0.80 完全是运气。

### 4.3 结论

- **准确率上四组没有显著差别**（单次划分 0.90 / 0.80 / 0.90 / 0.95）。
  最容易犯的错是看到 B 组 0.80 就说"增强有害"、看到 D 组 0.95 就说
  "RandomErasing 有效"——**两个结论都是错的**：20 张验证集上 1 张 = 5%，
  而多种子重复实验（上表右侧）显示，同一配置换一份划分就能有 4~7 个百分点的波动，
  远大于配置之间的差距。这也是指导书那句"小数据集上三个模型准确率可能相差不大"
  的同一个坑：**100 张图、单次划分、单次训练，任何小于 10% 的差距都不该下强结论。**
  这一步真正得到的结论是"**差异测不出来**"，而不是"谁更好"。
- **置信度分布才是增强真正的痕迹，而且必须池化后才看得清**。
  单次划分下"置信度 > 0.9 的样本数"是 7/12/9/7，同样落在噪声里；
  把 5 折 × 20 张池化成 100 个样本后，信号才稳定下来（详见第九节思考题 2）：
  平均置信度随增强强度**单调下降**（0.8097 → 0.8093 → 0.7981 → **0.7833**），
  而"判对样本 − 判错样本"的置信度差反而**从 +0.2154 拉大到 +0.2546**。
  也就是说，增强并没有让模型"更敢下判断"，而是让它**少一点盲目自信、
  但把对的和对错的分得更开**——这才是决策边界变平整的正确姿势。
- **代价是实打实的**：每轮训练时间 1.9s → 2.9s → 3.3s → 3.4s（+53% ~ +79%），
  而且增强越强，单轮之内模型见过的"有效样本多样性"越高，
  5 轮就明显不够了——C 组第 5 轮验证损失仍在下降（0.2502），
  说明它还没收敛。在 CPU 上，**增强的时间成本是确定的，准确率收益是不确定的**，
  这个账要算清楚。
- 指导书提醒"增强只作用于训练集，验证集必须用干净的 Resize + Normalize"，
  代码里是用 `get_loaders(train_transform=...)` 保证的：
  训练子集和验证子集是两个独立的 `FileNameDataset` 实例，套不同 transform，
  再用同一组 `random_split` 索引切分，所以**划分完全相同、预处理不同**。

---

## 五、第四步：`torchvision.models` 封装模型横向对比

**代码**：`step4_compare.py`　**日志**：`results/logs/step4_compare.log`

三个模型都是"ImageNet 预训练 + 冻结特征层 + 只训分类头"，
同样的划分（seed=42）、`epochs=5, lr=1e-3, batch_size=16`。

### 5.1 结果

| 模型 | 参数量 | 准确率 | 精确率 | 召回率 | F1 | 每轮用时 | 总用时 | 权重体积 |
|---|---|---|---|---|---|---|---|---|
| **resnet18** | 11,177,538 | **0.9000** | 0.9231 | 0.8889 | 0.8958 | 1.9 s | 9.7 s | 42.71 MB |
| **mobilenet_v3_small** | 1,519,906 | **0.9000** | 0.9231 | 0.8889 | 0.8958 | 2.2 s | 10.8 s | **5.93 MB** |
| shufflenet_v2_x1_0 | 1,255,654 | 0.7500 | 0.7500 | 0.7525 | 0.7494 | 2.4 s | 12.2 s | **4.96 MB** |

### 5.2 填表与评价

| 模型 | 参数量 | 准确率 | 每轮训练用时 | 我的评价 |
|---|---|---|---|---|
| resnet18 | 11M | 0.90 | 1.9 s | 结构规整、最稳，但权重 42.7MB，是另外两个的 7~9 倍；**它 96% 的体积都在那个 512→2 之外的老式 `fc` 里，部署到手机明显吃亏** |
| mobilenet_v3_small | 2.5M(表) / 1.52M(实测) | 0.90 | 2.2 s | 准确率与 resnet18 打平，体积只有 1/7，**本实验的最优性价比**，不愧是给手机端设计的深度可分离卷积 |
| shufflenet_v2_x1_0 | 2.3M(表) / 1.26M(实测) | 0.75 | 2.4 s | 最小最轻，但这次明显掉队 |

> 参数量一列指导书给的是约数（2.5M / 2.3M），实测（含 2 类新分类头）是
> 1,519,906 / 1,255,654。**注意 resnet18 的 11.18M 是指南数值，我这边实测一致**。

### 5.3 结论

- **准确率不是选型依据**：resnet18 与 mobilenet_v3_small 的四个指标**完全相同**
  （0.9000 / 0.9231 / 0.8889 / 0.8958）——它们在这 20 张验证集上错的是同一批图。
  这种情况下，**推理速度、模型体积、端侧友好度才是关键**，
  而这正好为第六步部署到手机做铺垫。
- **shufflenet 为什么只有 0.75？** 三条可能原因，从代码里可以逐一排查：
  1. 它的 `fc` 只有 `512→2`（1,026 个可训练参数），而它的特征层是为
     ImageNet 1000 类训练出来的**通道混洗 + 分组卷积**结构，
     冻结后表达能力受限，5 轮只够把线性头调个大概；
  2. 它的预训练特征更"压缩"，在只有 80 张训练图的情况下，
     小分类头的收敛本来就慢；
  3. 从日志看它的训练损失 5 轮只从 0.694 降到 0.659，**几乎没动**——
     这说明瓶颈不在过拟合，而是**优化没走起来**（也可能是 lr=1e-3 对
     它 2050 个可训练参数偏小/偏大）。
  验证方法很简单：把 epochs 提到 20、或对 shufflenet 单独调 lr，看曲线是否打开。
- 一个反直觉但重要的事实：**更小的模型在 CPU 上并不更快**。
  mobilenet/shufflenet 参数量只有 resnet18 的 1/8，每轮却更慢（2.2s / 2.4s vs 1.9s）。
  原因是深度可分离卷积的**算子数多、单算子计算量小**，在 CPU 上访存开销
  比算力开销更致命。这说明"参数量"和"速度"在 CPU 上是两回事，
  真要在手机上跑，得看目标平台的算子库（NNAPI / CoreML）而不是参数量。

---

## 六、第五步：模型保存与单张图片测试

**代码**：`step5_1_train_best.py` / `step5_2_test.py`　**日志**：`results/logs/`

### 6.1 只保存参数（推荐做法）

`torch.save(net.state_dict(), ...)` 而不是 `torch.save(net, ...)`：
只存张量字典，文件小、跨 torch 版本稳、不把代码路径序列化进去。
加载时先按同样结构建模型再 `load_state_dict`，这就是
"训练与推理分离"的工程标配。本实验产出的权重：

| 文件 | 内容 | 体积 |
|---|---|---|
| `results/best_model.pth` | 验证集最佳那一轮的参数 | 42.71 MB |
| `results/last_model.pth` | 最后一轮参数（用于对比） | 42.71 MB |
| `results/model_resnet18.pth` 等 | 第四步三个模型 | 42.71 / 5.93 / 4.96 MB |
| `results/model_step1_alexnet_scratch.pth` | 第一步从零训练的 AlexNet | 178 MB |
| `results/model_step2_*.pth` | 第二步冻结/微调两版 AlexNet | 各 217 MB |

### 6.2 训练时保存最佳模型（早停思想）

每轮在验证集上评估，**只有准确率严格超过历史最佳才写盘**，因此磁盘上的
`best_model.pth` 一定是泛化最好的那一版：

| 轮次 | 验证准确率 | 是否写盘 |
|---|---|---|
| 1 | 0.8000 | ✅ 保存 |
| 2 | 0.8500 | ✅ 保存 |
| 3 | **0.9000** | ✅ 保存 |
| 4 | 0.9000 | ❌ 未超过 |
| 5 | 0.9000 | ❌ 未超过 |

最佳准确率 **0.9000（第 3 轮）**，最后一轮也是 0.9000；
本次"最佳"与"最后"恰好同分，但工程上不能赌这一点——
如果第 4、5 轮开始过拟合，没有早停就会把变差的模型交出去。

### 6.3 单张图片推理测试

推理脚本里**没有** `models.resnet18(weights=DEFAULT)`——
用的是 `weights=None`，结构一致即可，完全不下载权重，也不需要训练代码：

```powershell
python step5_2_test.py results/best_model.pth datasets/cat.1.jpg
```
```
权重: results/best_model.pth (42.71 MB)
图片: datasets/cat.1.jpg
预测: cat 猫，置信度 78.82%
各类概率: 猫 78.82% | 狗 21.18%
```

再跑一次全量 100 张（含 80 张训练图和 20 张验证图）作为回归测试：

**全量准确率 0.9100（91/100）**，其中 9 张错分：

| 文件 | 真值 | 预测 | 置信度 | 可能的原因 |
|---|---|---|---|---|
| cat.5.jpg | 猫 | 狗 | 61.86% | 175×144 的模糊老照片，主体是键盘和桌面，暹罗猫只露一个头 |
| cat.19.jpg | 猫 | 狗 | 59.20% | 一堆小猫挤在毯子里，多目标 + 严重遮挡，没有"一只猫"的整体轮廓 |
| cat.23.jpg | 猫 | 狗 | 62.27% | 黑猫趴在猫爬架上，主体只占画面一小块，背景是置物架 |
| cat.26.jpg | 猫 | 狗 | 62.12% | 人和黑猫贴脸合影，猫只占右上角 |
| cat.44.jpg | 猫 | 狗 | 53.42% | **107×102 的极小图**，放大到 224 后细节全糊 |
| cat.45.jpg | 猫 | 狗 | 50.46% | 小猫埋在彩色拼布毯子里，只露出头和一只爪子 |
| dog.71.jpg | 狗 | 猫 | 63.77% | 柯基混血正面照：耳朵尖立、脸小，形态上确实接近猫 |
| dog.77.jpg | 狗 | 猫 | 64.37% | 两只博美被抱着，毛量大、脸小，同样"像猫" |
| dog.86.jpg | 狗 | 猫 | 53.91% | 牧羊犬极近特写，只剩一张脸，肤色纹理偏猫 |

**规律很清晰**：错分集中在
①目标太小 / 被遮挡 / 多目标（cat.19/23/26/45）；
②分辨率过低、放大后信息丢失（cat.5/44）；
③长相本身介于两类之间（dog.71/77/86）。
而且**9 张里有 8 张置信度都在 50%~65% 之间**——模型其实"心里没底"，
这就是为什么第六步的界面要把概率一起显示出来，而不是只给一个结论。

另外还有 11 张**判对了但置信度 < 60%**（cat.25/32/36/48、dog.59/61/78/80/92/95/98）。
它们同样落在上面三类里，只是刚好越过了 0.5 的分界线。
如果这是一个真实业务，这些"低置信样本"应该被挑出来交给人工复核——
这也是"只看准确率"会漏掉的价值。

---

## 七、第六步：封装成可拍照分类的小程序

**代码**：`step6_app.py`（Gradio，指导书方案）/ `step6_api.py`（FastAPI，思考题 3）

### 7.1 Gradio 版（已实测启动）

```powershell
.venv2\Scripts\python.exe step6_app.py
```

启动后**已实测验证**：

| 项目 | 结果 |
|---|---|
| 本机访问 | `http://127.0.0.1:7860` → HTTP **200**，页面含 Gradio 标记，标题"🐱🐶 猫狗分类器" |
| 手机访问（同一 Wi-Fi） | `http://192.168.2.116:7860` （本机局域网 IP，端口 7860，`server_name="0.0.0.0"`） |
| 输入组件 | `gr.Image(sources=["webcam", "upload"])`——**自带摄像头拍照** |
| 输出组件 | 文本结论 + `gr.Label` 各类别概率条 |

界面返回示例（直接调用 `predict()` 的实测结果）：

```
cat.1.jpg   -> 识别结果：🐱 猫（置信度 78.82%） | {'🐱 猫': 0.788, '🐶 狗': 0.212}
dog.51.jpg  -> 识别结果：🐶 狗（置信度 93.52%） | {'🐱 猫': 0.065, '🐶 狗': 0.935}
cat.19.jpg  -> 识别结果：🐶 狗（置信度 59.20%） | {'🐱 猫': 0.408, '🐶 狗': 0.592}   ← 错分样本，置信度也确实低
```

### 7.2 部署三要素的对应

| 要素 | 实现 |
|---|---|
| 加载训练好的权重 | 模块顶层 `net.load_state_dict(torch.load(WEIGHT, weights_only=True))`，**启动时只做一次** |
| 接收用户输入 | `gr.Image(type="pil", sources=["webcam","upload"])` |
| 返回预测结果 | `softmax` 转概率 → 取 `argmax` → 返回文本 + 概率字典 |

用 `torch.load(..., weights_only=True)` 而不是默认值，是为了避免反序列化
可能执行任意代码的安全隐患（训练/推理分离之后，权重文件在工程上属于"外部输入"）。

---

## 八、六步总表

| 步骤 | 核心改动 | 指导书预期 | **本实验实测** | 每轮用时 | 核心结论 |
|---|---|---|---|---|---|
| 1 手动搭建 + 从零训练 | 随机权重, 30 epoch | 0.65~0.80 | **0.85**（峰值 0.90） | 3.1 s | 训练损失 0.76→0.0012 而测试损失卡在 0.63，**过拟合** |
| 2 +ImageNet 预训练 | 灌权重、冻结卷积 | 0.90~1.00 | **1.00**（第 3 轮起） | 2.4 s | 3 轮就满分，**迁移学习是小数据集的最优解** |
| 2b 进阶：全部微调 | 解冻, lr=1e-5, 5 轮 | — | 0.90（还在上升） | 3.2 s | 不是更差，是**没训够**；57M 参数全动容易跑出预训练解空间 |
| 3 +数据增强 | 四组增强强度（含 RandomErasing） | ≈步骤 2 或更稳 | 单次 0.90/0.80/0.90/0.95；**5 折均值 0.90/0.88/0.91/0.89** | 1.9~3.4 s | 配置差距（≤0.03）**小于折间波动（0.04~0.07）**，准确率差异测不出来；真正变化在**置信度分布**与训练开销 |
| 4 多模型对比 | resnet18 / mobilenet / shufflenet | 都在 0.9+ | 0.90 / 0.90 / **0.75** | 1.9~2.4 s | 小数据下准确率分不出高下，**选型看体积与端侧友好度** |
| 5 保存与推理 | state_dict + 单图测试 | — | 全量 100 张 **0.91** | — | 训练与推理分离；9 张错分图揭示了模型的**能力边界** |
| 6 部署小程序 | Gradio 摄像头界面 | — | HTTP 200 实测通过 | — | 模型只有被用起来才有价值 |

**一句话总括**：这 100 张图上的核心矛盾从来不是"训练多久"，而是
**"知识从哪来"（第一步 vs 第二步）+ "评估有多可信"（20 张验证集的统计噪声）**。
第三步和思考题 2 把后半句做实了：4 种增强配置的差距（≤0.03）**小于**
换一份数据划分带来的波动（0.04~0.07），只有 5 折池化之后，
置信度分布上的差异才显出稳定的方向。

---

## 九、思考题

### 思考题 1：把冻结改为全模型微调（lr=1e-5），准确率和训练时间如何变化？

已在第二步实测（`results/step2_summary.json`）：

| 方案 | 准确率 | 每轮用时 | 总用时 |
|---|---|---|---|
| 冻结卷积，只训分类头（30 轮） | **1.0000** | 2.4 s | 72.8 s |
| 全模型微调（5 轮, lr=1e-5） | 0.9000 | 3.2 s | 16.0 s |

* **训练时间**：每轮从 2.4s 涨到 3.2s（+33%）。涨幅不大的原因是 AlexNet 里
  可训练参数本来就有 95.6% 在 `classifier`——"冻结卷积"冻结的只是那 2.5M。
  如果是 ResNet 那种卷积占绝对多数的结构，冻结与微调的耗时差距会显著得多。
* **准确率**：5 轮内微调**更差**（0.90 < 1.00），但逐轮曲线是
  0.65 → 0.75 → 0.85 → 0.90 → 0.90 的**单调上升**，而冻结方案第 3 轮就封顶了。
  所以正确结论是：**在训够的前提下微调不会更差，只是 5 轮 + lr=1e-5
  的预算不足以让它追上"3 轮就收敛"的冻结方案**。
* 附加观察：微调第 1 轮准确率反而掉到 0.65，训练损失 0.75——
  这是"全参数一起动，把 ImageNet 学到的特征短暂打散"的典型现象，
  也是为什么微调必须用小学习率、且要有足够的轮数让它重新收敛。

### 思考题 2：把数据增强再加强（如 `RandomErasing`），观察置信度分布变化

**已实测**，不是设想。代码：`step3_augment.py`（新增 D 组 `aug_transform("erase")`）
与 `step3_erasing_cv.py`（多种子重复实验）；日志 `results/logs/step3_erasing_cv.log`，
原始数据 `results/step3_erasing_cv.json`。

#### 实验设计

两个刻意的选择，都是为了不重蹈第三步"20 张验证集下不了结论"的坑：

1. **`RandomErasing` 放在 `Normalize` 之后，`value=0`。**
   它只能作用在 Tensor 上，所以必须排在 `ToTensor()` 后面；再往后挪到
   `Normalize` 之后，`value=0` 填充的就是 ImageNet 均值色（≈中性灰），
   相当于挖掉一块灰斑，而不是引入训练集里本不存在的"黑色矩形"伪影。
   `p=0.5, scale=(0.02, 0.2), ratio=(0.3, 3.3)` 用的是 torchvision 的常规取值。
2. **4 配置 × 5 个数据划分种子（42~46）× 5 epoch**，
   再把 5 折的验证集预测**池化**成 100 个样本来统计置信度分布。
   只看单次划分的 20 张，抽样误差约 ±0.05，根本分辨不出配置差异。

#### 结果一：准确率——差异测不出来

| 配置 | 准确率（5 折均值 ± 标准差） | 最低 | 最高 | 每轮(s) | 逐折准确率 |
|---|---|---|---|---|---|
| A 无增强 | 0.9000 ± 0.0612 | 0.85 | 1.00 | 2.5 | 0.90 / 0.90 / 1.00 / 0.85 / 0.85 |
| B 基础增强 | 0.8800 ± 0.0570 | 0.80 | 0.95 | 2.9 | 0.80 / 0.90 / 0.95 / 0.90 / 0.85 |
| C 强增强 | **0.9100 ± 0.0652** | 0.80 | 0.95 | 3.2 | 0.90 / 0.80 / 0.95 / 0.95 / 0.95 |
| D 强增强+RandomErasing | 0.8900 ± **0.0418** | 0.85 | 0.95 | 3.3 | 0.95 / 0.90 / 0.85 / 0.90 / 0.85 |

四组均值挤在 0.88~0.91 的 3 个百分点里，而**每组自己的折间标准差就有 4~7 个百分点**。
配置差距 < 划分波动 ⇒ 准确率上**没有可检出的差异**。
单次划分看到的"D 组 0.95 最高"（表 C）是运气，不是效应。

唯一值得记一笔的是：**D 组的标准差最小（0.0418，约为 A 组的 2/3）**，
即"加了擦除之后，换个数据划分结果更稳"。但 5 折的样本量也只能说"有这个迹象"。

#### 结果二：置信度分布——预期被推翻了

| 配置 | 平均置信度 | 中位数 | 判对样本均值 | 判错样本均值 | 判对−判错 | >0.9 | <0.6 |
|---|---|---|---|---|---|---|---|
| A 无增强 | 0.8097 | 0.8655 | 0.8313 | 0.6158 | +0.2154 | 32 | 11 |
| B 基础增强 | 0.8093 | 0.8589 | 0.8378 | 0.6003 | +0.2375 | 39 | 16 |
| C 强增强 | 0.7981 | 0.8449 | 0.8207 | 0.5696 | +0.2511 | 32 | 16 |
| D 强增强+RandomErasing | **0.7833** | 0.8260 | 0.8113 | 0.5567 | **+0.2546** | **22** | 15 |

置信度分箱（样本数，每组 n=100）：

| 配置 | 0.5~0.6 | 0.6~0.7 | 0.7~0.8 | 0.8~0.9 | 0.9~1.0 |
|---|---|---|---|---|---|
| A 无增强 | 11 | 14 | 12 | 31 | 32 |
| B 基础增强 | 16 | 6 | 21 | 18 | 39 |
| C 强增强 | 16 | 9 | 15 | 28 | 32 |
| D 强增强+RandomErasing | 15 | 12 | 18 | 33 | 22 |

#### 结论

1. **预期被推翻**：原以为 `RandomErasing` 会把平均置信度**进一步推高**，
   实测恰好相反——平均置信度随增强强度**单调下降**
   （0.8097 → 0.8093 → 0.7981 → 0.7833），D 组比 A 组低了 2.6 个百分点，
   而"置信度 > 0.9"的过度自信样本从 32/100 掉到 **22/100**。
2. **但"两极分化"的方向是对的，只是分化的方式不是"对的更自信"**：
   D 组把"判对 − 判错"的置信度差**从 +0.2154 拉大到 +0.2546**——
   整体不那么自信了，可是**对错之间的可分离度反而更大**。
   这正是数据增强想要的：模型不再靠记住某块局部区域（被擦掉就慌），
   而是依据更全局、更本质的证据来下判断。
   如果这是真实业务，"置信度 < 0.6"的样本（D 组 15/100）就是该交人工复核的那批。
3. **方法论上的收获比结论本身更重要**：单次划分下"置信度 > 0.9 的样本数"
   是 7/12/9/7，看起来 B 组（基础增强）最自信（12/20 = 60%）；
   池化到 100 个样本后 B 组只有 39/100 = 39%，而 C 组（32）与 A 组（32）持平。
   两次统计给出的**排序并不一致**——再一次说明 20 张验证集上的分布统计不可信，
   必须多种子/多折池化。

### 思考题 3：用 FastAPI 把 predict 封装成 HTTP 接口，写真正的微信小程序前端调用

已实现并实测通过：`step6_api.py`。

```
GET  /health    -> {"status": "ok"}
POST /predict   -> multipart/form-data（字段名 file）-> JSON
GET  /          -> 内置极简测试页（带 <input type="file" capture="environment">，手机可调摄像头）
```

用 `fastapi.testclient.TestClient` 做过的端到端实测（`tools/verify_step6.py`）：

```
GET  /health  -> 200 {'status': 'ok'}
POST /predict -> cat.1.jpg   200  cat 猫  置信度 78.82%
POST /predict -> dog.51.jpg  200  dog 狗  置信度 93.52%
GET  /        -> 200  HTML 长度 534，含上传控件
```

返回体设计成前端友好的形式：

```json
{
  "label": "cat 猫",
  "label_id": 0,
  "confidence": 0.788206,
  "probs": {"cat 猫": 0.788206, "dog 狗": 0.211794}
}
```

**微信小程序端**（`wx.uploadFile` 与 `POST /predict` 的 multipart 格式天然对应）：

```javascript
wx.chooseMedia({
  count: 1, mediaType: ['image'], sourceType: ['camera', 'album'],
  success(res) {
    wx.uploadFile({
      url: 'http://192.168.2.116:8000/predict',   // 换成本机局域网 IP
      filePath: res.tempFiles[0].tempFilePath,
      name: 'file',
      success(r) {
        const data = JSON.parse(r.data)
        wx.showModal({
          title: '识别结果',
          content: `${data.label}（置信度 ${(data.confidence * 100).toFixed(1)}%）`
        })
      }
    })
  }
})
```

要注意的工程细节：
1. **域名与协议**：微信小程序正式环境要求 HTTPS + 已备案域名，
   开发阶段需在"开发设置 → 服务器域名"里把本机 IP 加进白名单，
   或在开发者工具里勾选"不校验合法域名"。局域网 HTTP 只适合课堂演示。
2. **端口**：`step6_api.py` 用的是 8000，`step6_app.py` 用的是 7860，两者可以同时跑
   （`CATDOG_API_PORT` / `CATDOG_PORT` 环境变量可改）。
3. **推理逻辑复用**：`step6_app.py` 和 `step6_api.py` 的 `predict` 完全同源
   ——都是从 `results/best_model.pth` 加载同一个 ResNet18、用同一套
   `Resize(224) + Normalize` 预处理。这就是"训练/推理分离"的真正收益：
   换前端（Gradio → FastAPI → 小程序）不需要动模型一行代码。

---

## 十、与指导书的差异、踩坑记录

代码严格沿用了指导书的结构、超参和函数命名。为了"能跑起来 + 可复现"，
做了以下调整，**每一处都在对应脚本的 docstring 里写明**：

### 10.1 必须改的（指导书原样会报错）

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| 1 | 第三步参考代码 `net.load_state_dict(official.state_dict(), strict=False)` | `strict=False` 只容忍"多键/少键"，**不容忍形状不匹配**，官方 `fc.weight` 是 `[1000,512]` 而 `ResNet18Manual(num_classes=2)` 是 `[2,512]` → `RuntimeError: size mismatch for fc.weight` | 按指导书第二步处理 AlexNet 的同一套路：先建 1000 类结构灌权重，再 `net.fc = nn.Linear(512, 2)` |
| 2 | 第三步 `from step1_scratch import ResNet18Manual` | 指导书第一步讲的是 **AlexNet**，`step1_scratch.py` 里并没有 `ResNet18Manual`，这行 import 按原文必然 `ImportError` | 在 `step1_scratch.py` 中补上 `ResNet18Manual`（`BasicBlock` 结构、层名、参数形状与 `torchvision.models.resnet18` **逐键一致**，实测 `set(state_dict) 相等且形状全同`），使该行 import 与后续加载按原文即可运行 |
| 3 | 第一步 `base_path = r"D:\A枣庄学院工作\..."` | 老师电脑上的绝对路径 | 换成本仓库的 `datasets/` |

### 10.2 为满足"控制变量"补的

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| 4 | 第一步 `dataloader()` 的 `random.sample` | **没有固定随机种子**，每次运行划分出的 20 张测试图都不同，直接违反指导书自己写的"全程固定同一份数据划分（seed=42）" | 补 `random.seed(42)` |
| 5 | `FileNameDataset.__init__` 的 `os.listdir` | 返回顺序不保证稳定；即使 `random_split` 的种子固定，两次运行若目录顺序不同，切出来的仍是不同图片 | 改为 `sorted(os.listdir(...))`，并配 `tools/smoke_test.py` 验证"train=80 / val=20 / 交集=0" |

### 10.3 工程化补充（不改变任何计算结果）

* 训练日志用 `common.tee_stdout()` 同时落盘到 `results/logs/*.log`，每步实验的原始输出可回溯。
* 逐 epoch 的指标与耗时写进 `results/*_history.json`，报告里的每个数字都可查。
* 预训练权重缓存固定到仓库内 `.torch_cache`（`TORCH_HOME`），不写系统盘。
* `tools/smoke_test.py` 在跑长训练之前先验证数据管线、模型前向、
  手写 ResNet18 与 torchvision 的键名/形状是否一致。
* `tools/summarize.py` 把 `results/*.json` 自动汇总成报告表格，避免手抄数字出错。
* 第六步额外提供了 `step6_api.py`（思考题 3）与 `tools/verify_step6.py`（自动验证）。
* 思考题 2 额外提供了 `step3_erasing_cv.py`：4 种增强配置 × 5 个数据划分种子，
  把"配置差异"和"划分波动"分开，并把验证集预测池化成 100 个样本来统计置信度分布。

### 10.4 环境踩坑

1. **系统 Python 没有 torch**，且沙箱只允许写仓库目录，因此依赖装进仓库内 `.venv2`。
2. **`python -m venv .venv2`（带 pip）失败**，`ensurepip` 步骤报 `WinError 5 拒绝访问`；
   改用 `python -m venv --without-pip .venv2` + 系统 pip 的
   `--target .venv2\Lib\site-packages`，问题消失。
3. **PyPI 直连下载 torch 超时**（`ReadTimeoutError`），换清华镜像
   `https://pypi.tuna.tsinghua.edu.cn/simple` 后续全部顺利。
4. **`pip install --target ... --upgrade` 会在删除旧 numpy 目录时
   因 `libopenblas64__v0.3.21-gcc_10_3_0.dll` 被占用而 `PermissionError`**，
   导致 `requests`/`urllib3` 没被搬过去、`gradio` 导入失败；
   去掉 `--upgrade` 重跑即可只补装缺失的包。
5. **PowerShell 里把 Python 命令接管道（`| Out-String` 等）会被沙箱拒绝**
   （`Program 'python.exe' failed to run: Access is denied`）；
   改成直接执行或 `cmd /c "... > file"` 重定向即可。
6. `torch.load` 在 torch 2.4 会报 `FutureWarning`（`weights_only` 默认值将翻转），
   统一改成 `torch.load(..., weights_only=True)`。
7. 中间还发现并修掉了一个自己写的 bug：`step3_augment.py` 第一版沿用了指导书
   那行会报错的 `load_state_dict(strict=False)`，第一次运行以
   `size mismatch for fc.weight` 失败，修正后重跑通过。
8. **Windows 控制台默认 GBK，`print` 非 GBK 字符会直接抛 `UnicodeEncodeError`。**
   复验时用 `python tools\smoke_test.py` 输出最后一行 `全部通过 ✔` 就崩了；
   `tools\summarize.py` 里的 `→` 在被重定向成 `> results\summary_tables.md` 时
   也会崩（README 给的正是这条命令）。处理办法写在 `common._make_stdout_robust()`：
   **交互式控制台**保留 GBK 以避免中文乱码、只把无法编码的字符降级成 `?`；
   **输出被重定向**时切成 UTF-8，保证中文与符号无损落盘。
   `tools\summarize.py` 因为不能引入 torch（否则汇总要等好几秒），
   单独复制了同样的几行。

---

## 十一、复现方式

```powershell
# 0) 依赖（首次）
python -m venv --without-pip .venv2
python -m pip install --target .venv2\Lib\site-packages `
    -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# 1) 数据集与体检
.venv2\Scripts\python.exe tools\build_dataset.py     # 按 hash 复制并逐一校验
.venv2\Scripts\python.exe tools\check_images.py      # 解码/尺寸/模式体检
.venv2\Scripts\python.exe tools\smoke_test.py        # 数据管线与模型前向冒烟

# 2) 六步实验（或直接 pwsh -File tools\run_all.ps1）
.venv2\Scripts\python.exe step1_scratch.py
.venv2\Scripts\python.exe step2_pretrained.py
.venv2\Scripts\python.exe step3_augment.py
.venv2\Scripts\python.exe step3_erasing_cv.py   # 思考题2，4 配置 × 5 种子，CPU 约 5~6 分钟
.venv2\Scripts\python.exe step4_compare.py
.venv2\Scripts\python.exe step5_1_train_best.py
.venv2\Scripts\python.exe step5_2_test.py results\best_model.pth datasets\cat.1.jpg
.venv2\Scripts\python.exe step5_2_test.py results\best_model.pth --all

# 3) 汇总成报告表格
.venv2\Scripts\python.exe tools\summarize.py > results\summary_tables.md

# 4) 部署
.venv2\Scripts\python.exe step6_app.py     # http://127.0.0.1:7860（手机同 Wi-Fi 用本机 IP）
.venv2\Scripts\python.exe step6_api.py     # http://127.0.0.1:8000/docs
.venv2\Scripts\python.exe tools\verify_step6.py   # 不启服务，直接验证两种实现
```

### 产物清单

| 路径 | 说明 |
|---|---|
| `results/summary_tables.md` | 报告里所有表格的自动汇总 |
| `results/logs/*.log` | 每一步的完整训练输出 |
| `results/*_history.json` | 逐 epoch 指标、耗时、逐样本预测与概率 |
| `results/step5_all_predictions.json` | 全量 100 张的逐张预测结果 |
| `results/step3_erasing_cv.json` | 思考题 2：4 配置 × 5 种子的准确率与池化置信度分布 |
| `results/best_model.pth` | 部署用的最佳权重 |
| `results/trainDatasets.txt` / `testDatasets.txt` | 第一、二步的数据划分索引 |
